<%- include("../header") %>
<div class="container mt-0 mt-sm-3">
    <%- include("../menu") %>

    <div class="content row m-0 p-3">
        <div class="gray row p-2">
            
        </div>
        <div class="gray mt-3" id="tablazat" style="overflow-x:scroll">
            <h2>Szia</h2>
        </div>
            
    </div>
    <%- include('../foot') %>
</div>

<script>
    let users = [];
    $.post("./getAllUserData",
    {
        
    },
        //TODO szedd ki a jelszó küldést backenden!!!
        function(data, status){
            console.table(data.users)
            if(!data.success) return $("#tablazat").html("<h2>Hiba az adatok lekérésekor</h2>");
            users = data.users;
            $("#tablazat").html(toTable(data.users, true, ["Id", "Név", "E-mail", "Jogok", "Token", "Magasság", "Súly", "Cél súly"]))
            
        }
    );

    function edit(index) {
        console.log(users[index]);        
    }
</script>

<%- include("../footer") %>

